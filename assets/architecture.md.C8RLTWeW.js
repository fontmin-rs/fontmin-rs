import{U as e,gt as t,nt as n,rt as r}from"./chunks/framework.B3obAz7o.js";var i=JSON.parse(`{"title":"Architecture","description":"","frontmatter":{},"headers":[],"relativePath":"architecture.md","filePath":"architecture.md","lastUpdated":1784608400000}`),a={name:`architecture.md`};function o(e,i,a,o,s,c){return t(),n(`div`,null,[...i[0]||=[r(`<h1 id="architecture" tabindex="-1">Architecture <a class="header-anchor" href="#architecture" aria-label="Permalink to “Architecture”">​</a></h1><p>fontmin-rs is a Rust workspace with three public surfaces: a Rust CLI, a Node.js package, and a browser WASM package. Font parsing, subsetting, conversion, and CSS generation live in shared Rust crates; the JavaScript packages provide typed runtime selection, plugin orchestration, and platform-specific I/O.</p><h2 id="directory-structure" tabindex="-1">Directory Structure <a class="header-anchor" href="#directory-structure" aria-label="Permalink to “Directory Structure”">​</a></h2><table tabindex="0"><thead><tr><th>Path</th><th>Responsibility</th></tr></thead><tbody><tr><td><code>crates/fontmin</code></td><td>Public Rust facade for direct transforms and shared types</td></tr><tr><td><code>crates/fontmin_{core,detect,ttf}</code></td><td>Assets, formats, metadata, format detection, and sfnt/TTF primitives</td></tr><tr><td><code>crates/fontmin_{subset,otf,woff,woff2,eot,svg,css}</code></td><td>Subsetting, format conversion, icon fonts, and CSS generation</td></tr><tr><td><code>crates/fontmin_{config,fs,pipeline,plugin,plugins}</code></td><td>Serializable config, path expansion, pipeline engine, and built-ins</td></tr><tr><td><code>crates/fontmin_{diagnostics,testing}</code></td><td>Shared errors, test fixtures, and fixture constructors</td></tr><tr><td><code>apps/fontmin</code></td><td>Rust <code>fontmin-rs</code> CLI</td></tr><tr><td><code>napi/fontmin</code></td><td>N-API bridge published as <code>@fontmin-rs/binding</code></td></tr><tr><td><code>packages/fontmin</code></td><td>Public <code>fontmin-rs</code> Node.js package, config loader, plugins, and cache</td></tr><tr><td><code>wasm/fontmin-core</code></td><td><code>wasm-bindgen</code> bridge that compiles the Rust facade to WebAssembly</td></tr><tr><td><code>wasm/fontmin</code></td><td>Public <code>@fontmin-rs/wasm</code> browser package and in-memory pipeline</td></tr><tr><td><code>npm/*</code></td><td>Platform-specific native binding packages</td></tr><tr><td><code>fixtures</code></td><td>Fixed TTF and OTF inputs shared by Rust, Node.js, WASM, and docs tests</td></tr><tr><td><code>docs</code></td><td>Bilingual VitePress site and browser playground</td></tr><tr><td><code>scripts</code></td><td>Release metadata, package smoke, and artifact checks</td></tr></tbody></table><h2 id="runtime-data-flow" tabindex="-1">Runtime Data Flow <a class="header-anchor" href="#runtime-data-flow" aria-label="Permalink to “Runtime Data Flow”">​</a></h2><div class="language-mermaid line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">mermaid</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">flowchart TD</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  files[&quot;Font files or in-memory bytes&quot;]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  cli[&quot;Rust CLI&quot;]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  node[&quot;Node optimize(config)&quot;]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  browser[&quot;Browser optimizeBrowser(config)&quot;]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  selector[&quot;Node runtime selector&quot;]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  napi[&quot;N-API binding&quot;]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  wasm[&quot;WASM bridge&quot;]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  pipeline[&quot;Rust pipeline&quot;]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  core[&quot;Shared Rust font crates&quot;]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  outputs[&quot;Font assets, CSS, and metadata&quot;]</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  files --&gt; cli --&gt; pipeline</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  files --&gt; node --&gt; selector</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  selector --&gt;|native| napi</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  selector --&gt;|wasm| wasm</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  files --&gt; browser --&gt; wasm</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  napi --&gt; core</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  wasm --&gt; core</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  pipeline --&gt; core</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  core --&gt; outputs</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br><span class="line-number">14</span><br><span class="line-number">15</span><br><span class="line-number">16</span><br><span class="line-number">17</span><br><span class="line-number">18</span><br><span class="line-number">19</span><br><span class="line-number">20</span><br><span class="line-number">21</span><br></div></div><p>The CLI runs the Rust pipeline directly. The Node package performs path and glob expansion, cache access, output writes, and custom JavaScript hooks in Node.js, while one selected runtime handles every built-in font operation. The browser package has no filesystem layer and sends named in-memory assets through the WASM bridge.</p><h2 id="package-and-runtime-boundaries" tabindex="-1">Package and Runtime Boundaries <a class="header-anchor" href="#package-and-runtime-boundaries" aria-label="Permalink to “Package and Runtime Boundaries”">​</a></h2><p>The N-API and WASM bridges expose the same direct operations: subsetting, TTF/WOFF/WOFF2/EOT/SVG conversion, CFF/CFF2 OTF conversion, inspection, and CSS generation. Platform packages under <code>npm/*</code> contain native binaries; they are optional dependencies of <code>fontmin-rs</code>, so <code>runtime: &#39;auto&#39;</code> can recover from a missing native artifact by loading the packaged WASM module.</p><p>One <code>optimize()</code> call never mixes native and WASM built-ins. <code>native</code> is the default, <code>wasm</code> forces WebAssembly, and <code>auto</code> falls back only when the native binding cannot load. Invalid fonts, unsupported options, and conversion errors do not trigger a second attempt in another runtime. Custom Node plugins remain in Node.js regardless of the selected runtime.</p><p><code>@fontmin-rs/wasm</code> is asynchronous and memory-only. It supports direct helpers, the <code>optimizeBrowser()</code> pipeline, built-in conversion plugins, Unicode delivery slices, presets, and custom asset transforms, but it has no path inputs, glob expansion, disk cache, output directory, or filesystem hooks.</p><h2 id="configuration-boundary" tabindex="-1">Configuration Boundary <a class="header-anchor" href="#configuration-boundary" aria-label="Permalink to “Configuration Boundary”">​</a></h2><p>The Rust CLI and Node package share this automatic discovery order:</p><ol><li><code>fontmin.config.ts</code></li><li><code>fontmin.config.mts</code></li><li><code>fontmin.config.mjs</code></li><li><code>fontmin.config.cjs</code></li><li><code>fontmin.config.json</code></li><li><code>fontmin.config.jsonc</code></li></ol><p>The Rust CLI parses JSON and JSONC directly in Rust. It evaluates executable module configs in a short-lived Node.js 22+ child process, then deserializes the result and runs the Rust pipeline. Module configs are trusted project code: they are not sandboxed and inherit the current environment and working directory.</p><p>The module bridge accepts a default or named <code>config</code> export containing an object, or a synchronous or asynchronous function returning one. It accepts JSON-compatible data and serializable descriptors from built-in plugins, <code>modernWeb()</code>, and <code>fontminCompatPreset()</code>. Custom JavaScript hooks, function-valued <code>css.fontFamily</code>, unknown built-ins, and unsupported built-in options are rejected with a field path such as <code>plugins[1].transform</code>.</p><p>After either loader reads a config file, an omitted <code>cwd</code> defaults to that file&#39;s directory. Relative inputs, output and cache directories, and configured <code>textFile</code> paths therefore resolve from the config directory. The Rust CLI then applies command-line overrides. The Rust and Node schemas have a shared baseline but also runtime-specific fields; the <a href="./guide/config#configuration-models">configuration guide</a> lists those differences explicitly.</p><h2 id="current-boundaries" tabindex="-1">Current Boundaries <a class="header-anchor" href="#current-boundaries" aria-label="Permalink to “Current Boundaries”">​</a></h2><ul><li>OTF inspection supports static CFF, CFF2, and glyf-backed OpenType inputs. OTF-to-TTF conversion emits a static TrueType <code>glyf</code> font and removes CFF2 and variation tables; Type 2 hinting is not retained.</li><li>WOFF2 inspection validates the header and table directory before reading sfnt metadata. WOFF2-to-TTF decoding is available through both bridge APIs, while the synchronous Node helper remains native-only.</li><li><code>modernWeb()</code> emits WOFF, WOFF2, and CSS. EOT and SVG require explicit plugins, the compatibility preset, or matching CLI output formats.</li><li>Rust CLI module configs can use serializable built-in descriptors, but they cannot execute arbitrary JavaScript plugin hooks inside the Rust pipeline.</li></ul>`,19)]])}var s=e(a,[[`render`,o]]);export{i as __pageData,s as default};