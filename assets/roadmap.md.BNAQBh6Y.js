import{U as e,gt as t,nt as n,rt as r}from"./chunks/framework.B3obAz7o.js";var i=JSON.parse(`{"title":"Roadmap to 1.0","description":"","frontmatter":{},"headers":[],"relativePath":"roadmap.md","filePath":"roadmap.md","lastUpdated":1784608400000}`),a={name:`roadmap.md`};function o(e,i,a,o,s,c){return t(),n(`div`,null,[...i[0]||=[r(`<h1 id="roadmap-to-1-0" tabindex="-1">Roadmap to 1.0 <a class="header-anchor" href="#roadmap-to-1-0" aria-label="Permalink to “Roadmap to 1.0”">​</a></h1><p>fontmin-rs <code>0.1.0-beta.2</code> is published across the CLI, Node.js package, browser WASM package, native binding, and eight platform packages. The public surface is broad enough for real projects; the remaining work is about proving stability, correctness, and operability before declaring the contracts final.</p><p>This roadmap uses exit criteria instead of calendar promises. A milestone is complete only when its checks are repeatable on <code>main</code> and in the release workflow.</p><h2 id="current-baseline" tabindex="-1">Current baseline <a class="header-anchor" href="#current-baseline" aria-label="Permalink to “Current baseline”">​</a></h2><ul><li>One release version is validated across all 11 npm packages, Cargo metadata, embedded runtime versions, the changelog, and the release tag.</li><li>CI covers formatting, warning-free Rust and TypeScript linting, Node.js 22/24/26, WASM, browser loading, the documentation playground, native package smoke tests, release readiness, and benchmarks.</li><li>The release gate rejects high or critical dependency advisories, requires at least 80% Rust line coverage, inspects packed npm contents, and runs consumer smoke tests.</li><li>The shared binary fixture inventory and its checksums are now validated by <code>pnpm run fixtures:check</code>.</li><li>Local development and every GitHub workflow use the same pinned Rust 1.97.1 toolchain; upgrades are explicit repository changes.</li><li>A portable benchmark snapshot lives in <a href="../benchmarks/baseline.json"><code>benchmarks/baseline.json</code></a>; CI publishes each new run as an artifact without using noisy hosted-runner timings as a hard gate.</li></ul><h2 id="beta-hardening" tabindex="-1">Beta hardening <a class="header-anchor" href="#beta-hardening" aria-label="Permalink to “Beta hardening”">​</a></h2><p>The next beta should reduce unknowns rather than add a large new API surface.</p><ul><li>Expand the licensed fixture corpus with a compact CJK font, a representative icon font, and malformed regression inputs. Keep provenance, licenses, and SHA-256 digests in the manifest.</li><li>Add native-versus-WASM conformance cases for every built-in transform and preset, including error diagnostics and output metadata.</li><li>Turn parser and table-boundary failures discovered by fuzzing into permanent corpus cases; run a bounded fuzz/sanitizer job on a schedule.</li><li>Profile the compatibility <code>glyph + ttf2woff</code> pipeline. The beta.2 debug baseline is slower than classic Fontmin on the recorded machine; reach parity or explain an intentional correctness tradeoff.</li><li>Define the Rust MSRV separately from the pinned CI toolchain, and document a deliberate toolchain upgrade cadence.</li></ul><p>Exit criterion: two consecutive beta releases pass the complete release gate without manual metadata repair or platform-package rollback.</p><h2 id="release-candidate" tabindex="-1">Release candidate <a class="header-anchor" href="#release-candidate" aria-label="Permalink to “Release candidate”">​</a></h2><p>The release candidate freezes user-facing contracts and changes the focus to compatibility evidence.</p><ul><li>Freeze the CLI flags and exit codes, configuration schema, Node/WASM exports, plugin lifecycle, diagnostic codes, and generated file naming rules.</li><li>Publish a support matrix for Node.js versions, operating systems, CPU/libc targets, browser WASM capabilities, and the Rust MSRV for library consumers.</li><li>Define a deprecation policy and require migration notes for every breaking change from the final beta.</li><li>Compare representative Fontmin pipelines for glyph coverage, parsed output, CSS semantics, and file naming; byte-for-byte equality is not required.</li><li>Re-record benchmarks on a fixed runner with release-profile bindings. Treat a sustained regression greater than 10% as release-blocking after confirming it in at least three like-for-like runs.</li><li>Exercise install, CLI, ESM, browser, native fallback, and forced-WASM paths from packed tarballs rather than the workspace.</li></ul><p>Exit criterion: the frozen contract and support matrix survive one release candidate cycle with no unresolved P0/P1 correctness, security, or packaging issue.</p><h2 id="_1-0-release-gate" tabindex="-1">1.0 release gate <a class="header-anchor" href="#_1-0-release-gate" aria-label="Permalink to “1.0 release gate”">​</a></h2><p>1.0 is ready when all of the following are true:</p><ul><li>Public API and configuration contracts are documented and covered by compatibility tests.</li><li>Every supported font path either produces parseable output with the requested coverage or returns a stable, actionable diagnostic; malformed input never panics across the public boundary.</li><li>Native packages and the WASM fallback pass the same conformance corpus on all advertised targets.</li><li>Rust line coverage remains at least 80%, lint is warning-free, packed-package smoke tests pass, and no high or critical dependency advisory is accepted.</li><li>Release-profile performance is at least at parity with classic Fontmin for the representative compatibility pipeline and remains inside the agreed regression budget for native subset and web-font conversion.</li><li>The release workflow can publish every package, create the GitHub release, and verify npm dist-tags from a clean tag without local intervention.</li><li>Migration, troubleshooting, security reporting, release, and rollback documentation are complete.</li></ul><p>Work not required for 1.0—such as every historical Fontmin plugin, every font format edge case, or distributed caching—should remain explicitly documented as post-1.0 scope instead of delaying stable contracts indefinitely.</p>`,17)]])}var s=e(a,[[`render`,o]]);export{i as __pageData,s as default};