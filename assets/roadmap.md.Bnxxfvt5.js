import{U as e,gt as t,nt as n,rt as r}from"./chunks/framework.B3obAz7o.js";var i=JSON.parse(`{"title":"Roadmap to the first stable release","description":"","frontmatter":{},"headers":[],"relativePath":"roadmap.md","filePath":"roadmap.md","lastUpdated":1785085496000}`),a={name:`roadmap.md`};function o(e,i,a,o,s,c){return t(),n(`div`,null,[...i[0]||=[r(`<h1 id="roadmap-to-the-first-stable-release" tabindex="-1">Roadmap to the first stable release <a class="header-anchor" href="#roadmap-to-the-first-stable-release" aria-label="Permalink to “Roadmap to the first stable release”">​</a></h1><p>fontmin-rs <code>0.1.0-rc.1</code> is published across the CLI, Node.js package, browser WASM package, native binding, and eight platform packages. The public surface is broad enough for real projects; the remaining work is about proving stability, correctness, and operability before declaring the contracts final.</p><p>This release line targets <code>0.1.0</code> as the first stable version. It does not claim SemVer <code>1.0.0</code> compatibility; a future 1.0 release will run its own release candidate cycle.</p><p>This roadmap uses exit criteria instead of calendar promises. A milestone is complete only when its checks are repeatable on <code>main</code> and in the release workflow.</p><h2 id="current-baseline" tabindex="-1">Current baseline <a class="header-anchor" href="#current-baseline" aria-label="Permalink to “Current baseline”">​</a></h2><ul><li>One release version is validated across all 11 npm packages, Cargo metadata, embedded runtime versions, the changelog, and the release tag.</li><li>CI covers formatting, warning-free Rust and TypeScript linting, Node.js 22/24/26, WASM, browser loading, the documentation playground, native package smoke tests, release readiness, and benchmarks.</li><li>The release gate rejects high or critical dependency advisories, requires at least 80% Rust line coverage, inspects packed npm contents, and runs consumer smoke tests.</li><li>The shared binary fixture inventory and its checksums are now validated by <code>pnpm run fixtures:check</code>.</li><li>Local development and every GitHub workflow use the same pinned Rust 1.97.1 toolchain; upgrades are explicit repository changes.</li><li>The <a href="./benchmarks">performance policy</a> builds release-profile bindings on a pinned CI software runner, aggregates three trials, and gates the paired compatibility pipeline while retaining absolute timings for diagnosis.</li><li>The licensed fixture corpus includes Latin, compact CJK, icon-font, CFF, CFF2, variable-font, and malformed inputs with reproducible provenance.</li><li>Native and WASM run one semantic conformance matrix across every built-in transform, preset, output metadata contract, and malformed diagnostic.</li><li>A bounded AddressSanitizer cargo-fuzz target runs on relevant changes and a weekly schedule; minimized crashes become permanent malformed fixtures.</li><li>Rust 1.88.0 is the separately declared and tested MSRV. The pinned toolchain and upgrade cadence are defined in the <a href="./support">support policy</a>.</li><li>The release-profile <code>glyph + ttf2woff</code> baseline is about 6.73 times faster than classic Fontmin on the recorded machine; the former debug-profile measurement has been retired.</li><li>The <a href="./deprecation">deprecation policy</a>, <a href="./troubleshooting">troubleshooting guide</a>, <a href="https://github.com/fontmin-rs/fontmin-rs/security/policy" target="_blank" rel="noreferrer">security policy</a>, migration guide, and release rollback procedure define the maintenance path from prerelease through 1.0.</li><li>Rust advisory checks have no accepted exceptions; current npm audit findings are resolved by scoped, lockfile-tested overrides.</li></ul><h2 id="beta-hardening-—-complete" tabindex="-1">Beta hardening — complete <a class="header-anchor" href="#beta-hardening-—-complete" aria-label="Permalink to “Beta hardening — complete”">​</a></h2><p>Beta.3 and beta.4 passed the same complete release gate consecutively. Beta.4 was tagged from the already-green candidate commit and required no release-time metadata repair, code fix, or platform-package rollback. The permanent malformed corpus remains open to minimized fuzz discoveries and real-world failures.</p><p>Exit criterion: two consecutive beta releases pass the complete release gate without manual metadata repair or platform-package rollback.</p><h2 id="_0-1-release-candidate" tabindex="-1">0.1 release candidate <a class="header-anchor" href="#_0-1-release-candidate" aria-label="Permalink to “0.1 release candidate”">​</a></h2><p>The release candidate freezes user-facing contracts and changes the focus to compatibility evidence.</p><ul><li>The <a href="./contracts">machine-readable public contract</a> freezes CLI flags and exit codes, configuration schemas, Node/WASM exports, plugin lifecycle, diagnostic codes, and generated file naming rules.</li><li>The <a href="./support">support matrix</a> publishes Node.js versions, operating systems, CPU/libc targets, browser WASM capabilities, and the Rust MSRV.</li><li>Compare representative Fontmin pipelines for glyph coverage, parsed output, CSS semantics, and file naming; byte-for-byte equality is not required.</li><li>Install, CLI, ESM, browser, native, native fallback, and forced-WASM paths are exercised from packed tarballs rather than workspace imports.</li></ul><p>Exit criterion: the frozen contract and support matrix survive one release candidate cycle with no unresolved P0/P1 correctness, security, or packaging issue.</p><h2 id="_0-1-0-stable-release-gate" tabindex="-1">0.1.0 stable release gate <a class="header-anchor" href="#_0-1-0-stable-release-gate" aria-label="Permalink to “0.1.0 stable release gate”">​</a></h2><p><code>0.1.0</code> is ready when all of the following are true:</p><ul><li>Public API and configuration contracts are documented and covered by compatibility tests.</li><li>Every supported font path either produces parseable output with the requested coverage or returns a stable, actionable diagnostic; malformed input never panics across the public boundary.</li><li>Native packages and the WASM fallback pass the same conformance corpus on all advertised targets.</li><li>Rust line coverage remains at least 80%, lint is warning-free, packed-package smoke tests pass, and no high or critical dependency advisory is accepted.</li><li>Release-profile performance is at least at parity with classic Fontmin for the representative compatibility pipeline and remains inside the agreed regression budget for native subset and web-font conversion.</li><li>The release workflow can publish every package, create the GitHub release, and verify npm dist-tags from a clean tag without local intervention.</li></ul><p>Work not required for <code>0.1.0</code>—such as every historical Fontmin plugin, every font format edge case, or distributed caching—should remain explicitly documented as post-0.1 scope instead of delaying the first stable release indefinitely.</p><h2 id="path-to-1-0" tabindex="-1">Path to 1.0 <a class="header-anchor" href="#path-to-1-0" aria-label="Permalink to “Path to 1.0”">​</a></h2><p>After <code>0.1.0</code>, compatibility evidence from real projects, additional font fixtures, and maintained dependency replacements will shape the 1.0 scope. The documented RC contract remains governed by the deprecation policy. A future <code>1.0.0</code> release begins with a separately versioned RC instead of treating the <code>0.1.0</code> candidate cycle as 1.0 validation.</p>`,19)]])}var s=e(a,[[`render`,o]]);export{i as __pageData,s as default};