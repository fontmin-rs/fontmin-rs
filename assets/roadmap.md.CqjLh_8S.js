import{U as e,gt as t,nt as n,rt as r}from"./chunks/framework.B3obAz7o.js";var i=JSON.parse(`{"title":"Roadmap after 1.0","description":"","frontmatter":{},"headers":[],"relativePath":"roadmap.md","filePath":"roadmap.md","lastUpdated":1786704273000}`),a={name:`roadmap.md`};function o(e,i,a,o,s,c){return t(),n(`div`,null,[...i[0]||=[r(`<h1 id="roadmap-after-1-0" tabindex="-1">Roadmap after 1.0 <a class="header-anchor" href="#roadmap-after-1-0" aria-label="Permalink to “Roadmap after 1.0”">​</a></h1><p>fontmin-rs <code>1.0.1</code> is the stable release across the CLI, Node.js package, browser WASM package, native binding, and eight platform packages. New work keeps that contract stable while consolidating the internal modules that enforce it.</p><h2 id="_1-0-1-—-harden-the-stable-contract-completed" tabindex="-1">1.0.1 — harden the stable contract (completed) <a class="header-anchor" href="#_1-0-1-—-harden-the-stable-contract-completed" aria-label="Permalink to “1.0.1 — harden the stable contract (completed)”">​</a></h2><ul><li>Keep English and Chinese status documents aligned with the active stable release line, and make release checks reject stale status claims.</li><li>Deepen the runtime-neutral optimizer module so Node and browser pipelines share built-in dispatch, clone ordering, delivery-slice behavior, and preset semantics.</li><li>Deepen the canonical sfnt reader so format adapters do not repeat table alignment, range, overlap, or payload lookup mechanics.</li><li>Keep built-in plugin descriptor knowledge behind one seam instead of leaking raw names and option records into transform, cache, and workspace modules.</li><li>Keep the native and WASM operation inventory synchronized through their existing two-adapter seam.</li><li>Add production assertions for the semantic properties already promised by fixtures, including preserved variable-font tables after subsetting.</li></ul><p>Exit criterion: the public <code>1.0</code> contract is unchanged, Node and browser pipelines pass the same observable conformance cases, canonical font validation has one test surface, and the complete release gate passes.</p><h2 id="_1-0-2-candidate-—-close-option-semantic-gaps" tabindex="-1">1.0.2 candidate — close option-semantic gaps <a class="header-anchor" href="#_1-0-2-candidate-—-close-option-semantic-gaps" aria-label="Permalink to “1.0.2 candidate — close option-semantic gaps”">​</a></h2><ul><li>Enforce the documented <code>preserveHinting</code>, <code>keepNotdef</code>, and <code>keepLayout</code> behavior at the Rust core boundary.</li><li>Run the same observable option cases through native Node, browser WASM, and the Rust CLI configuration path.</li><li>Keep OTF and SVG hinting compatibility fields non-breaking while documenting exactly which hint data cannot be translated or generated.</li><li>Retain the Rust 1.88 MSRV and record any local dependency metadata override with an owner and explicit upstream exit condition.</li><li>Regenerate compatibility, dependency, size, coverage, and release metadata evidence from the exact release candidate before publication.</li></ul><p>Exit criterion: no documented option is silently ignored, native and WASM produce equivalent observable results, the Rust 1.88 and full release gates pass, and registry compatibility evidence is attached to the candidate.</p><p>Published <code>1.0.2-rc.1</code> evidence now covers the option semantics, native/WASM/CLI conformance, Rust 1.88, dependency and security policy, artifact budgets, coverage, package smoke tests, the clean CI and release matrices, all eight native artifacts, the candidate performance baseline, and the exact npm-registry compatibility report. The candidate gate is complete; stable promotion remains a separate observation and reviewed-audit decision.</p><h2 id="_1-1-entry-criteria" tabindex="-1">1.1 entry criteria <a class="header-anchor" href="#_1-1-entry-criteria" aria-label="Permalink to “1.1 entry criteria”">​</a></h2><p><code>1.1</code> is reserved for additive public behavior backed by real consumer evidence. A proposal needs a reproducible consumer case, native/WASM conformance fixtures, documented performance budgets where relevant, and no change to existing <code>1.0</code> behavior. Internal refactors and compatibility fixes remain patch releases.</p><h2 id="completed-path-to-1-0" tabindex="-1">Completed path to 1.0 <a class="header-anchor" href="#completed-path-to-1-0" aria-label="Permalink to “Completed path to 1.0”">​</a></h2><p>The milestones below preserve the completed plan and its exit evidence. They use exit criteria instead of calendar promises: a milestone is complete only when its checks are repeatable on <code>main</code>, packed-package smoke tests cover the affected public paths, and the release workflow remains reproducible.</p><h2 id="stable-baseline" tabindex="-1">Stable baseline <a class="header-anchor" href="#stable-baseline" aria-label="Permalink to “Stable baseline”">​</a></h2><ul><li>One release version is validated across all 11 npm packages, Cargo metadata, embedded runtime versions, the changelog, and the release tag.</li><li>CI covers formatting, warning-free Rust and TypeScript linting, Node.js 22/24/26, WASM, browser loading, documentation, native package smoke tests, release readiness, and benchmarks.</li><li>The release gate rejects high or critical dependency advisories, requires at least 80% Rust line coverage, inspects packed npm contents, and runs consumer smoke tests.</li><li>Native and WASM share a semantic conformance corpus across built-in transforms, presets, output metadata, and malformed diagnostics.</li><li>A bounded AddressSanitizer cargo-fuzz target runs on relevant changes and a weekly schedule; minimized crashes become permanent malformed fixtures.</li><li>Rust <code>1.88.0</code> is the tested MSRV. Development and release automation use the pinned toolchain declared by the repository.</li></ul><h2 id="_0-1-1-—-contract-correction" tabindex="-1">0.1.1 — contract correction <a class="header-anchor" href="#_0-1-1-—-contract-correction" aria-label="Permalink to “0.1.1 — contract correction”">​</a></h2><p>The first patch release removes drift discovered immediately after <code>0.1.0</code> without expanding the public API.</p><ul><li>Align README files, installation guides, navigation labels, and the machine-readable inventory with the stable release.</li><li>Make the packaged npm CLI accept every frozen Rust CLI flag and verify both executables from the same contract.</li><li>Make the default SVG icon-font stem consistently <code>iconfont</code> across Rust, Node.js, and browser pipelines.</li><li>Add semantic checks that prevent stable package versions from publishing prerelease installation guidance.</li></ul><p>Exit criterion: the full release gate passes, packed npm and Rust CLI behavior matches the public inventory, and <code>0.1.1</code> is published to <code>latest</code>.</p><h2 id="_0-2-—-consolidate-pipeline-boundaries" tabindex="-1">0.2 — consolidate pipeline boundaries <a class="header-anchor" href="#_0-2-—-consolidate-pipeline-boundaries" aria-label="Permalink to “0.2 — consolidate pipeline boundaries”">​</a></h2><p>The <code>0.2</code> line reduces duplicate policy while preserving the stable public entry points.</p><ul><li>Turn the npm executable into a thin adapter over shared command parsing and pipeline behavior instead of maintaining a second independent CLI.</li><li>Normalize built-in plugin configuration into typed domain values in <code>fontmin_config</code>; remove repeated JSON option decoding from <code>fontmin_pipeline</code>.</li><li>Replace well-known <code>AssetMeta.custom</code> keys with typed metadata while keeping an extension map for third-party plugins.</li><li>Split the Node optimizer by pipeline execution, transform rules, and filesystem/cache ownership while retaining the current <code>optimize()</code> facade.</li><li>Split oversized CLI and Node integration tests by public command or API seam.</li><li>Decide whether Rust workspace crates are internal-only or independently publishable, then make Cargo manifests enforce that decision.</li><li>Keep the pre-<code>0.1.0</code> design proposal clearly historical; current architecture and contract documents remain authoritative.</li></ul><p>Exit criterion: each public entry point retains its contract, duplicate CLI and configuration rules have one source of truth, and the full conformance and package gates pass without compatibility exceptions.</p><h2 id="_0-3-—-real-world-resilience-and-performance" tabindex="-1">0.3 — real-world resilience and performance <a class="header-anchor" href="#_0-3-—-real-world-resilience-and-performance" aria-label="Permalink to “0.3 — real-world resilience and performance”">​</a></h2><p>The <code>0.3</code> line builds evidence from production-sized inputs and reduces remaining operational risk.</p><ul><li>Expand conformance fixtures for large CJK fonts, variable fonts, color fonts, malformed tables, and mixed delivery slices.</li><li>Add bounded memory and latency budgets for native and WASM processing, with regression reports that identify the responsible stage.</li><li>Grow fuzz corpora from real failures and run focused targets for parsers, converters, configuration loading, and output naming.</li><li>Audit duplicate compression and error-handling dependencies, vendored patches, binary size, and upstream replacement paths.</li><li>Validate cache concurrency, cancellation, and cleanup under interrupted multi-process builds.</li></ul><p>Exit criterion: representative large fonts stay within documented performance budgets, every supported format path has regression fixtures, and known vendored/dependency risks have an owner and replacement decision.</p><h2 id="_1-0-—-independently-validated-contract" tabindex="-1">1.0 — independently validated contract <a class="header-anchor" href="#_1-0-—-independently-validated-contract" aria-label="Permalink to “1.0 — independently validated contract”">​</a></h2><p><code>1.0.0</code> is not a rename of the <code>0.1</code> contract. It begins with its own release candidate after evidence from real projects has shaped the final surface.</p><ul><li>Collect compatibility reports from CLI, Node.js, and browser consumers.</li><li>Publish migration guidance for every intentional contract change.</li><li>Complete the announced deprecation windows and remove only APIs eligible under the deprecation policy.</li><li>Reconfirm supported runtimes, native targets, browser capabilities, Rust MSRV, diagnostics, and generated naming rules.</li><li>Pass at least one release-candidate cycle with no unresolved P0/P1 correctness, security, performance, or packaging issue.</li></ul><p><code>1.0.0-rc.1</code> completed this cycle with all standalone consumers and the exact tag boundary workflow passing. The reviewed readiness audit records no unresolved P0/P1 issue. Stable promotion accepted only the version constants required for <code>1.0.0</code>; the reviewed runtime behavior remained unchanged.</p><p>Exit criterion: the independently versioned RC contract survives the complete release gate and real-project validation, then the same reviewed runtime implementation is promoted to <code>1.0.0</code> without release-time repair.</p><h2 id="after-1-0" tabindex="-1">After 1.0 <a class="header-anchor" href="#after-1-0" aria-label="Permalink to “After 1.0”">​</a></h2><ul><li><code>1.0.x</code>: keep fixes compatibility-preserving, retain every minimized regression sample, and publish registry compatibility evidence for each stable release.</li><li><code>1.1</code>: accept additive APIs only when real consumer requests, conformance fixtures, and native/WASM performance budgets define the behavior.</li><li>Maintenance: continue replacing vendored or unmaintained dependencies when a verified upstream path exists, and keep contracts, docs, packages, and CI matrices checked from the same inventories.</li><li>A breaking release is planned only after the deprecation policy and a new independently reviewed compatibility cycle are complete.</li></ul>`,34)]])}var s=e(a,[[`render`,o]]);export{i as __pageData,s as default};